# Optional: a GitHub token raises API rate limits and unlocks contribution +
# pull-request counts (via the GraphQL API). A classic token with public_repo
# / read:user scope is enough. Without it, public REST data still works.
GITHUB_TOKEN=
